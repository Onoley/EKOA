import Link from "next/link";import{signOut}from"@/features/auth/actions";import{requireActiveProfile}from"@/features/auth/authorization";import{AdminNameBadge,ProfileAvatar}from"@/features/profile/avatar";import{getPublicEnv}from"@/lib/config/env";
const statusLabels:Record<string,string>={draft:"Brouillon",published:"Publiée",limited:"Diffusion limitée",under_review:"En cours d’examen",removed:"Retirée",archived:"Archivée"};
export default async function ProfilePage(){const{profile,supabase}=await requireActiveProfile();const env=getPublicEnv();const[{data:questions,error:questionsError},{data:followRows,error:followError},{data:categoryFollows},{data:verifiedFollows,error:verifiedError}]=await Promise.all([supabase.from("questions").select("id,text,status").eq("author_id",profile.user_id).order("created_at",{ascending:false}),supabase.from("question_follows").select("question_id").eq("user_id",profile.user_id).order("created_at",{ascending:false}),supabase.from("category_follows").select("category_id,categories(id,slug,name)").eq("user_id",profile.user_id),supabase.rpc("get_followed_verified_profiles")]);const followedIds=followRows?.map((row)=>row.question_id)??[];const{data:followedQuestions,error:followedQuestionsError}=followedIds.length?await supabase.from("questions").select("id,text").in("id",followedIds).eq("status","published").in("moderation_status",["clear","approved"]):{data:[],error:null};const hasError=questionsError||followError||followedQuestionsError||verifiedError;
 return <main className="mobile-page profile-page p-5"><div className="flex items-start justify-between gap-3"><div><h1 className="title">Profil</h1><p className="body-copy mt-1">Vos contenus et informations privées.</p></div><Link href="/profil/parametres" className="secondary-button compact" aria-label="Ouvrir les paramètres">⚙</Link></div><section className="profile-hero mt-6 rounded-3xl border border-black/10 p-5"><ProfileAvatar userId={profile.user_id} username={profile.username??"membre supprimé"} supabaseUrl={env.NEXT_PUBLIC_SUPABASE_URL} className="profile-page-avatar" verified={profile.account_type==="verified"} admin={profile.role==="admin"}/><div className="profile-name-row mt-4"><h2 className="text-xl font-bold">@{profile.username}</h2>{profile.role==="admin"?<AdminNameBadge/>:null}</div><p className="text-sm text-[var(--muted)]">{profile.role==="admin"?"Compte administrateur certifié":`Compte ${profile.account_type==="verified"?"vérifié":"ordinaire"}`}</p>{profile.role==="admin"?<Link href="/admin" className="admin-access-button mt-4">Ouvrir le tableau de bord admin <span aria-hidden="true">→</span></Link>:null}{profile.account_type==="verified"?<Link href="/profil/campagnes" className="secondary-button mt-4">Rapports sponsor</Link>:null}</section>{hasError?<div role="alert" className="error-state mt-5">Certaines informations du profil n’ont pas pu être chargées.</div>:null}
 <section className="mt-7" aria-labelledby="created"><div className="flex justify-between gap-3"><h2 id="created" className="text-xl font-bold">Questions créées</h2><Link href="/creer" className="secondary-button compact">Créer</Link></div>{questions?.length?<ul className="mt-4 space-y-3">{questions.map((q)=><li key={q.id} className="rounded-2xl border border-black/10 p-4"><p className="font-semibold">{q.text}</p><p className="mt-1 text-sm text-[var(--muted)]">{statusLabels[q.status]??q.status}</p><div className="mt-3 flex gap-2">{q.status==="draft"?<Link href={`/creer?draft=${q.id}`} className="secondary-button compact">Modifier</Link>:null}{q.status==="published"?<><Link href={`/questions/${q.id}`} className="primary-button compact">Voir</Link><Link href={`/creer?wave=${q.id}`} className="secondary-button compact">Nouvelle vague</Link></>:null}</div></li>)}</ul>:<div className="empty-state mt-4">Aucune question créée.</div>}</section>
 <section className="mt-7" aria-labelledby="followed"><h2 id="followed" className="text-xl font-bold">Questions suivies</h2>{followedQuestions?.length?<ul className="mt-4 space-y-3">{followedQuestions.map((q)=><li key={q.id}><Link href={`/questions/${q.id}`} className="block rounded-2xl border border-black/10 p-4 font-semibold">{q.text}</Link></li>)}</ul>:<div className="empty-state mt-4">Aucune question suivie.</div>}</section>
 <section className="mt-7" aria-labelledby="categories"><h2 id="categories" className="text-xl font-bold">Catégories suivies</h2>{categoryFollows?.length?<ul className="mt-4 flex flex-wrap gap-2">{categoryFollows.map((row)=>{const category=Array.isArray(row.categories)?row.categories[0]:row.categories;return category?<li key={category.id}><Link href={`/fil?category=${category.slug}`} className="secondary-button compact">{category.name}</Link></li>:null})}</ul>:<div className="empty-state mt-4">Aucune catégorie suivie.</div>}</section>
 <section className="mt-7" aria-labelledby="accounts"><h2 id="accounts" className="text-xl font-bold">Comptes vérifiés suivis</h2>{Array.isArray(verifiedFollows)&&verifiedFollows.length?<ul className="mt-4 space-y-3">{verifiedFollows.map((account:{user_id:string;username:string})=><li key={account.user_id}><Link href={`/profils/${account.username}`} className="block rounded-2xl border border-black/10 p-4 font-semibold">@{account.username} · Compte vérifié</Link></li>)}</ul>:<div className="empty-state mt-4">Aucun compte vérifié suivi.</div>}</section><form action={signOut} className="mt-8"><button className="secondary-button w-full">Se déconnecter</button></form></main>}
