"use client";import{useActionState}from"react";import{requestDeletion,type ProfileActionState}from"./actions";
export function DeletionForm(){const[state,action,pending]=useActionState(requestDeletion,{status:"idle",message:""} satisfies ProfileActionState);return <form action={action} className="mt-6"><label className="field-label" htmlFor="confirmation">Saisissez SUPPRIMER pour confirmer</label><input className="field-input" id="confirmation" name="confirmation" autoComplete="off" required/><button className="mt-4 inline-flex min-h-12 w-full items-center justify-center rounded-xl bg-[var(--danger)] px-5 font-semibold text-white outline-none focus-visible:ring-3 focus-visible:ring-[#ffc9ce]" disabled={pending}>{pending?"Enregistrement…":"Confirmer la demande"}</button>{state.message?<p role="alert" className="field-error">{state.message}</p>:null}</form>}
